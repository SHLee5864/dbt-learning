
version: 2

models:
  - name: my_first_dbt_model
    description: "A starter dbt model"
    columns:
      - name: id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null

  - name: my_second_dbt_model
    description: "A starter dbt model"
    columns:
      - name: id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null
  - name: my_third_dbt_model
    description: "id와 id*2 값을 가진 모델"
    columns:
      - name: id
        description: "원본 id"
        data_tests:
          - unique
          - not_null
      - name: id_doubled
        description: "id의 2배 값"